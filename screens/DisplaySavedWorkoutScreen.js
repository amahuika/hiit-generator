import { StyleSheet, Text, View } from "react-native";

function DisplaySavedWorkoutScreen(props) {
  return (
    <View>
      <Text>Workout</Text>
    </View>
  );
}
export default DisplaySavedWorkoutScreen;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    backgroundColor: "#222831",
  },
});
